{
  "name": "email_designer",
  "version": "1.0.0",
  "description": "Email Designer with Drag & Drop",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd mail_designer && yarn install",
    "dev": "cd mail_designer && yarn dev",
    "build": "cd mail_designer && yarn build"
    
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {  
  }
}
